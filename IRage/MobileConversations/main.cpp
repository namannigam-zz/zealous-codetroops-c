#include <iostream>

using namespace std;
#define MAX 100000
int main()
{
    int N,
    int T[MAX],X[MAX];
    cin>>N;
    int i,j;
    for(i=0;i<N;i++)
    {
        cin>>T[i]>>X[i];
    }
    return 0;
}
